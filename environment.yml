name: stock_ml_train
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - ipykernel # to enable jupyter notebook for local development
  - python=3.12
  - pip
  - pip:
      - -e .
      - -r requirements.txt
